// KarsHotel PMS — Prisma seed (placeholder for Phase A.5)
// Real seed will be implemented in Phase D (seedDemoHotel: 4 categories, 21 rooms, ~30 bookings).

async function main() {
  // eslint-disable-next-line no-console
  console.log('[seed] Phase A — no data to seed yet. Models will be added in Phase B.');
}

main()
  .then(() => process.exit(0))
  .catch((err: unknown) => {
    // eslint-disable-next-line no-console
    console.error(err);
    process.exit(1);
  });
